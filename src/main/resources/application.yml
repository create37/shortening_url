spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:shorteningUrlDb
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  jpa:
    show-sql: true

---
spring:
  profiles: test
  datasource:
    url: jdbc:h2:mem:placeTestDb
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  jpa:
    show-sql: true